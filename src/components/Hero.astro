<section class="hero" aria-labelledby="hero-heading">
  <div class="hero-narrative">
    <p class="hero-eyebrow">SYSTEMS · APIs · AUTOMATION</p>
    <h1 id="hero-heading">Python Backend Developer</h1>
    <p class="hero-description">
      I build backend systems, APIs and automation with Python to solve practical problems without
      unnecessary complexity.
    </p>
    <div class="hero-actions">
      <a
        class="hero-primary"
        href="https://job-radar-bonw.onrender.com/"
        target="_blank"
        rel="noopener noreferrer"
      >
        Explore Job Radar
      </a>
      <a
        class="hero-secondary"
        href="https://github.com/SrMetus"
        target="_blank"
        rel="noopener noreferrer"
      >
        View GitHub
      </a>
    </div>
  </div>
  <aside class="hero-focus" aria-labelledby="hero-focus-label">
    <p id="hero-focus-label" class="hero-focus-label">
      FOCUS
    </p>
    <p class="hero-focus-content">FastAPI · REST APIs · Automation</p>
  </aside>
</section>

<style>
  .hero {
    display: grid;
    gap: var(--space-12);
    padding-block: var(--space-8) var(--space-16);
  }

  .hero-narrative {
    min-inline-size: 0;
  }

  .hero-eyebrow,
  .hero-focus-label {
    color: var(--color-primary);
    font-size: var(--font-size-small);
    font-weight: var(--font-weight-semibold);
    letter-spacing: 0.06em;
  }

  .hero-eyebrow {
    margin-block-end: var(--space-6);
  }

  h1 {
    max-inline-size: 13ch;
    margin-block-end: var(--space-6);
    font-size: var(--font-size-hero);
    line-height: 1.1;
    letter-spacing: -0.035em;
  }

  .hero-description {
    max-inline-size: 48ch;
    margin-block-end: var(--space-8);
    font-size: var(--font-size-body-large);
  }

  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: var(--space-4) var(--space-6);
  }

  .hero-actions a {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-block-size: 3rem;
    font-weight: var(--font-weight-semibold);
    text-align: center;
  }

  .hero-primary {
    padding: var(--space-3) var(--space-6);
    border-radius: var(--radius-control);
    background: var(--color-primary);
    color: var(--color-surface);
    text-decoration: none;
  }

  .hero-primary:hover {
    background: var(--color-primary-hover);
    color: var(--color-surface);
    text-decoration: underline;
  }

  .hero-secondary {
    padding-block: var(--space-3);
  }

  .hero-focus {
    max-inline-size: 24rem;
    padding-block-start: var(--space-4);
    border-block-start: 1px solid var(--color-border);
  }

  .hero-focus-label {
    margin-block-end: var(--space-2);
  }

  .hero-focus-content {
    margin: 0;
    font-size: var(--font-size-body);
  }

  @media (min-width: 48rem) {
    .hero {
      gap: var(--space-16);
      padding-block: var(--space-12) var(--space-24);
    }
  }

  @media (min-width: 64rem) {
    .hero {
      grid-template-columns: minmax(0, 3fr) minmax(0, 1fr);
      align-items: end;
      gap: var(--space-16);
      padding-block: var(--space-16) var(--space-32);
    }

    .hero-focus {
      margin-block-end: var(--space-1);
    }
  }
</style>
